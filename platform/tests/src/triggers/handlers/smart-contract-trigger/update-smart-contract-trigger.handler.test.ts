/**
 * UpdateSmartContractTrigger handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: triggers
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { updateSmartContractTrigger } from "@ledgerfit/api-server/domains/triggers/handlers";
import type { TriggersDDDDependencies } from "@ledgerfit/api-server/domains/triggers";
import { createSmartContractTrigger as createSmartContractTriggerEntity } from "@ledgerfit/factories/triggers";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@ledgerfit/tests/shared";
import { TRIGGERS_USE_CASES } from "../__factories__/use-cases.js";

describe("UpdateSmartContractTrigger", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: TriggersDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<TriggersDDDDependencies>(
      TRIGGERS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with updated entity", async () => {
    const entity = createSmartContractTriggerEntity({ orgId });
    const input = {} as any;
    const updatedEntity = { ...entity, ...input };
    const useCaseResult = { data: updatedEntity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.params = { triggerId: entity.id };
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeUpdateSmartContractTrigger, "execute").mockResolvedValue(useCaseResult);

    await updateSmartContractTrigger(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeUpdateSmartContractTrigger.execute).toHaveBeenCalledWith(expect.objectContaining({ triggerId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
