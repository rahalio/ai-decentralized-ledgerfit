/**
 * CancelTrainingJob handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: training
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { cancelTrainingJob } from "@ledgerfit/api-server/domains/training/handlers";
import type { TrainingDDDDependencies } from "@ledgerfit/api-server/domains/training";
import { createUpdateCancelRequest } from "@ledgerfit/factories/training";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@ledgerfit/tests/shared";
import { TRAINING_USE_CASES } from "../__factories__/use-cases.js";

describe("CancelTrainingJob", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: TrainingDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<TrainingDDDDependencies>(
      TRAINING_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with updated entity", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const input = createUpdateCancelRequest();
    const updatedEntity = { ...entity, ...input };
    const useCaseResult = { data: updatedEntity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.params = { jobId: entity.id };
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeCancelTrainingJob, "execute").mockResolvedValue(useCaseResult);

    await cancelTrainingJob(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeCancelTrainingJob.execute).toHaveBeenCalledWith(expect.objectContaining({ jobId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
