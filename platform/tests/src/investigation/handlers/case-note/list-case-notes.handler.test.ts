/**
 * ListCaseNotes handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: investigation
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { listCaseNotes } from "@ledgerfit/api-server/domains/investigation/handlers";
import type { InvestigationDDDDependencies } from "@ledgerfit/api-server/domains/investigation";
import { createCaseNote as createCaseNoteEntity } from "@ledgerfit/factories/investigation";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@ledgerfit/tests/shared";
import { INVESTIGATION_USE_CASES } from "../__factories__/use-cases.js";

describe("ListCaseNotes", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: InvestigationDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<InvestigationDDDDependencies>(
      INVESTIGATION_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with list of entities", async () => {
    const entities = [
      createCaseNoteEntity({ orgId }),
      createCaseNoteEntity({ orgId }),
    ];
    const useCaseResult = {
      data: { items: entities, nextCursor: undefined, prevCursor: undefined },
      meta: { correlationId: "test", timestamp: new Date().toISOString(), pagination: {} }
    };
    
    vi.spyOn(mockDeps.executeListCaseNotes, "execute").mockResolvedValue(useCaseResult);

    await listCaseNotes(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeListCaseNotes.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
