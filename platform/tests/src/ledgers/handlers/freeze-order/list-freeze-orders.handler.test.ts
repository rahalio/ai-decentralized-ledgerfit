/**
 * ListFreezeOrders handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: ledgers
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { listFreezeOrders } from "@ledgerfit/api-server/domains/ledgers/handlers";
import type { LedgersDDDDependencies } from "@ledgerfit/api-server/domains/ledgers";
import { createFreezeOrder as createFreezeOrderEntity } from "@ledgerfit/factories/ledgers";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@ledgerfit/tests/shared";
import { LEDGERS_USE_CASES } from "../__factories__/use-cases.js";

describe("ListFreezeOrders", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: LedgersDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<LedgersDDDDependencies>(
      LEDGERS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with list of entities", async () => {
    const entities = [
      createFreezeOrderEntity({ orgId }),
      createFreezeOrderEntity({ orgId }),
    ];
    const useCaseResult = {
      data: { items: entities, nextCursor: undefined, prevCursor: undefined },
      meta: { correlationId: "test", timestamp: new Date().toISOString(), pagination: {} }
    };
    
    vi.spyOn(mockDeps.executeListFreezeOrders, "execute").mockResolvedValue(useCaseResult);

    await listFreezeOrders(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeListFreezeOrders.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
