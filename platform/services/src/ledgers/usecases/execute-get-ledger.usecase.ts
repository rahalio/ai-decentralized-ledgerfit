/**
 * ExecuteGetLedger - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: ledgers
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetLedger Use Case
 *
 * DDD: Application use case for get Ledger.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetLedgerInput, GetLedgerOutput } from "../dto/ledger.dto";
import type { ExecutionContextService, IdGeneratorService } from "@ledgerfit/services/_shared/index.js";
import type { LedgerRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetLedger {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly ledger: LedgerRepository
  ) {}

  async execute(input: GetLedgerInput): Promise<GetLedgerOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.ldgId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).ledgerId) {
      throw new ValidationError("ledgerId is required");
    }
    const ledgerId = (input as any).ledgerId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.ledger.getLedger({ ledgerId: ledgerId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${ledgerId}`);
    }

    return entity as GetLedgerOutput;
  }
}
