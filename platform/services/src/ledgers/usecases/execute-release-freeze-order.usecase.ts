/**
 * ExecuteReleaseFreezeOrder - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: ledgers
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteReleaseFreezeOrder Use Case
 *
 * DDD: Application use case for get Release.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ReleaseFreezeOrderInput, ReleaseFreezeOrderOutput } from "../dto/release.dto";
import type { ExecutionContextService, IdGeneratorService } from "@ledgerfit/services/_shared/index.js";
import type { ReleaseRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteReleaseFreezeOrder {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly release: ReleaseRepository
  ) {}

  async execute(input: ReleaseFreezeOrderInput): Promise<ReleaseFreezeOrderOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.ldgId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).freezeOrderId) {
      throw new ValidationError("freezeOrderId is required");
    }
    const freezeorderId = (input as any).freezeOrderId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.release.releaseFreezeOrder({ freezeOrderId: freezeorderId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${freezeorderId}`);
    }

    return entity as ReleaseFreezeOrderOutput;
  }
}
