/**
 * ExecuteCancelTrainingJob - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: training
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteCancelTrainingJob Use Case
 *
 * DDD: Application use case for update Cancel.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { CancelTrainingJobInput, CancelTrainingJobOutput } from "../dto/cancel.dto";
import type { ExecutionContextService, IdGeneratorService } from "@ledgerfit/services/_shared/index.js";
import type { CancelRepository } from "../ports";
import { NotFoundError, PermissionDeniedError, ValidationError } from "../errors";
import { CanUpdate } from "../policies/can-update.policy";
export class ExecuteCancelTrainingJob {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly cancel: CancelRepository
  ) {}

  async execute(input: CancelTrainingJobInput): Promise<CancelTrainingJobOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.trnId();

    // All field names extracted from OpenAPI spec
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).jobId) {
      throw new ValidationError("jobId is required");
    }
    const jobId = (input as any).jobId;

    // Check policy
    if (!CanUpdate.allow({} as any, "")) {
      throw new PermissionDeniedError("Operation not allowed");
    }

    // Cancel via repository (pass correlation ID for response meta)
    const updated = await this.cancel.cancelTrainingJob({
      ...input,
      orgId: this.context.getOrgId(),
      correlationId,
    } as any);
    if (!updated) {
      throw new NotFoundError(`Entity not found: ${jobId}`);
    }

    return updated as CancelTrainingJobOutput;
  }
}
