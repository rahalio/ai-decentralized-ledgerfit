/**
 * ExecuteGetRevealRequest - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: investigation
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetRevealRequest Use Case
 *
 * DDD: Application use case for get Reveal.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetRevealRequestInput, GetRevealRequestOutput } from "../dto/reveal.dto";
import type { ExecutionContextService, IdGeneratorService } from "@ledgerfit/services/_shared/index.js";
import type { RevealRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetRevealRequest {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly reveal: RevealRepository
  ) {}

  async execute(input: GetRevealRequestInput): Promise<GetRevealRequestOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.invId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).revealRequestId) {
      throw new ValidationError("revealRequestId is required");
    }
    const revealrequestId = (input as any).revealRequestId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.reveal.getRevealRequest({ revealRequestId: revealrequestId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${revealrequestId}`);
    }

    return entity as GetRevealRequestOutput;
  }
}
